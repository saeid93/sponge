apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: triton-image-podmonitor
spec:
  namespaceSelector:
    any: true
  podMetricsEndpoints:
  - path: /metrics
    port: metrics
  selector:
    matchLabels:
      app: triton-image

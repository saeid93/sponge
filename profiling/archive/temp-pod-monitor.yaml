apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: triton-podmonitor1
spec:
  selector:
    matchLabels:
      app: triton1
  podMetricsEndpoints:
    - port: metrics
      path: /metrics
  namespaceSelector:
    any: true
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: triton-podmonitor
spec:
  selector:
    matchLabels:
      app: triton
  podMetricsEndpoints:
    - port: metrics
      path: /metrics
  namespaceSelector:
    any: true
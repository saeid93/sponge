apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: triton-podmonitor-object
spec:
  selector:
    matchLabels:
      app: triton-object
  podMetricsEndpoints:
    - port: metrics
      path: /metrics
  namespaceSelector:
    any: true